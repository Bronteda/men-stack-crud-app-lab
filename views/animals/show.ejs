<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%=animal.name%></title>
  </head>
  <body>
    <%- include('../partials/nav') %>
    <div class="animal-details">
      <h1><%=animal.name%></h1>
        <% if (imageUrl) { %>
        <img src="<%= imageUrl %>" alt="<%= animal.name %>" style="max-width:250px;">
        <% } else { %>
          <p>No image available.</p>
        <% } %>
      <h2>Information:</h2>
       <div>
        <p><strong>Locations:</strong> <%= animal.location %></p>
        <p><strong>Habitat:</strong> <%= animal.habitat %></p>
        <p><strong>Diet:</strong> <%= animal.diet %></p>
        <p><strong>Lifespan:</strong> <%= animal.lifespan %></p>
      </div>
      <div class="animal-actions">
          <form class="action-form" action="/animals/<%=animal._id%>?_method=DELETE" method="POST">
            <button type="submit" class="action-btn delete">Delete <%= animal.name %></button>
          </form>

        <a href="/animals/<%=animal._id%>/edit" class="action-btn edit">Edit <%= animal.name %></a>
      </div>
    </div>
  </body>
</html>
